# Implementation review, 2026-09-19

Two-axis code review (Standards, Spec) since the scaffold commit `242dc0b`, a
deep-module review of every module, and Fable's own inspection of real outputs
(cards, drafts, slides, the first gate runs). Findings are listed with what
was done about them. Anything not fixed is a suggestion with a reason.

## Fixed, test-first

| Finding | Axis | Fix |
|---|---|---|
| The consented photo store was untracked (`.gitignore` matched `src/photos/`) and never wired into the license check | Standards, Spec | Ignore rule narrowed to `/photos/consented/`; `cli.ts` passes the store; fence rule 2 is now reachable |
| The spike published with no source line and no confirmation | Standards (fence 1, 8) | Spike caption carries a source line; `--yes` required |
| Approval binding was a tautology: the verdict copied the draft's own hash | Spec (verdict point 3) | The card prints `ref <12 hex>`; `readVerdict` reads it back from the message; `verdictMatches` compares by prefix |
| A single-slide draft could call `media_publish` twice after a crash | Spec (point 5) | New `checkSingle` phase polls the recorded container first; PUBLISHED marks published, FINISHED publishes once |
| `publishBy` was computed from "now + 36 h", not the assignment date; a 2 h floor could push it past a launch | Fable inspection, Spec | Anchored to 11:59:59 PT the day after the assignment; earliest of item deadline − 24 h, item start; no floor |
| Launches that lift off before the assignment day's 3 pm window were shortlisted | Fable inspection | Shortlist drops them with a note |
| The license rule accepted any host that claimed a CC tag; ESA was missing | Standards (fence 7) | Tag rule limited to Launch Library metadata and ESA hosts; NASA by host; ESA added |
| A dead model server never reached Discord | Spec (modules.md) | `ensureModelServer` is an injected, reported runner step |
| The token refresh clock never started on a fresh install | Spec (point 5) | First use writes the token file; setup guide adds `pnpm tool token-refresh` |
| The model copied "Friday's meeting, room 512" from a guide example, and typed the handle into the last slide | Fable inspection | Guide rewritten; handle stripped by the normalizer |
| The model got weekdays wrong from raw ISO timestamps | Fable inspection | Prompt renders every time as "Tue Sep 29, 6:10 PM PT (iso)" and the assignment's weekday |
| qwen code headless exceeded its 300 s budget on the 3.8 model | Fable measurement | `--safe-mode` (41 s → 22 s on a trivial prompt); 600 s budget; HTTP failover as a `Generate` adapter |
| `fetchItems` returned an array with a hidden `notes` property; `shortlist` recovered it by cast | Deep-module, Standards | Explicit `{ items, notes }`; `shortlist` takes `notes` in its options |
| Two time modules with the same DST code; the zone string in four places | Standards, Deep-module | One `src/newsroom/time.ts` |
| Vocabulary drift (`pick*`, `candidate`), dead exports, a lint script with no linter, a fake adapter in production code, tests past the interface | Standards, Deep-module | Renamed, deleted, moved |

## Suggestions, deliberately not done today

- **A composition module** (`src/newsroom/compose.ts`) so `daily`, `gate` and
  `publish` share one construction and the env-to-config mapping is testable.
  `cli.ts` is still the only untested policy holder. Worth doing in week two.
- **Narrow `DailyDeps`**: bind `home` and `dirs` at the root the way `fetch`
  already is, so the runner stops knowing directories exist (11 fields → 8).
- **Reuse one Chromium in the gate** (`RenderOptions.browser` exists and is
  never passed). Saves ~200 ms per run; cosmetic.
- **Inject the GitHub token** into the image host instead of shelling out to
  `gh auth token` at module level.
- **Two tests assert mechanism, not behaviour** (`render/index.test.ts` class
  names; `writer.test.ts` "reads the guides once" parsing the HTTP body). Keep
  until the writer accepts a `Generate` with recorded calls at its seam.
- **Gate acceptance cases** from the verdict (deadline before 3 pm, wake after
  an event's window, approved draft with an unresolved flag): the first two are
  unit tests now (`store/publish-by.test.ts`, `shortlist/publish-window.test.ts`,
  the publisher's expiry tests); the third is satisfied by design (approve means
  every flag was confirmed) and by the card test that prints every flag.
- **Fallback pillar in the runner**: when a sourced pillar has no material, the
  runner should switch to an explainer rather than let the model write a
  "quiet week" post that claims sources were scanned. Cheap; next.
- **Launch photos**: no Launch Library image passes the whitelist in real data.
  A NASA image search adapter (public domain) is the honest way to get photos
  on launch cards. Week two.
- **Opportunities and neighbors sources** are still stubs; the week-one plan
  avoids those pillars.

## Kept as is, on purpose

`publisher/machine.ts` (pure state machine, every crash path a literal-event
test), `store/index.ts` (sole owner of status changes), `license/index.ts`
("no photo" is a normal result), `shortlist/index.ts` (pure, every drop
explained), `render/index.ts` (`renderHtml` pure, `renderSlides` the browser),
the three vendor adapters, and `doctor/index.ts` (returns results, prints
elsewhere).

## Gate results and the harness measurement (2026-09-20)

First gate, qwen code harness primary, HTTP fallback, fixtures, real Qwen 3.8:
20 of 21 drafted; mean 179 s, slowest 455 s; the one failure was the qwen CLI
exiting 1 with its safe-mode banner, which was not yet classified as a
fallback-worthy harness failure (fixed, commit "qwen harness: a non-zero exit
with no payload is a harness failure").

Same prompt (launches, 4 items, 4.6k-char system + 2.2k-char user), measured
twice on the idle machine:

| Path | Wall time | Output | Note |
|---|---|---|---|
| qwen code headless, `--safe-mode`, tools off | 600 s, exit 55 | none | hit the budget |
| Direct HTTP, same model, `response_format` json_schema | 152 s | 1,705 tokens (1,230 reasoning) | succeeded |
| qwen code, empty shortlist | 49 s | 1,360 tokens | 8 s to first token, 34 tok/s |

Reading: the model's own thinking is most of the time on either path; the
harness adds its own context and turns on top, which on long prompts pushed
it past the budget. Over 23 harness attempts, 2 failed (one CLI exit, one
timeout); the HTTP path answered every time it was asked. Recommendation to
the account owner: make HTTP the default writer (`WRITER_MODEL=http`), keep
qwen code as the maintenance console and as an optional harness. ADR-0007's
"qwen code is the writer harness" clause would then be amended. The second
gate runs with `WRITER_MODEL=http` on the fixed code.
